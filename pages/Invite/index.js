import React from "react";
import { View, Text } from "react-native";

const InviteScreen = () => {
  return (
    <View
      style={{
        backgroundColor: "black",
        justifyContent: "center",
        alignItems: "center",
        width: "100%",
        height: "100%",
      }}
    >
      <Text style={{ color: "#fff", fontSize: "24px" }}>
        This is invite screen
      </Text>
    </View>
  );
};

export default InviteScreen;
